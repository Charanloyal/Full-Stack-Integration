services:
  - type: web
    name: full-stack-workspace
    env: node
    buildCommand: npm install && npm install --prefix backend && npm install --prefix frontend && npm run db:generate --prefix backend && npm run build --prefix backend && npm run build --prefix frontend
    startCommand: npm run db:push --prefix backend && node backend/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
